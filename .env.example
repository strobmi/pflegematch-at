# Kopiere diese Datei als .env auf dem VPS und fülle die Werte aus
POSTGRES_PASSWORD=ein-sicheres-passwort
NEXTAUTH_SECRET=generiere-mit-openssl-rand-base64-32
NEXTAUTH_URL=https://pflegematch.at

# Nur für lokale Entwicklung (nicht für Docker benötigt):
# DATABASE_URL=postgresql://pflegematch:pflegematch_dev@localhost:5432/pflegematch_dev
